fix: v0.5.2 - remove ghost "unnamed" skill after uninstall

Skills with missing/invalid name field in frontmatter default to "unnamed".
Previously this stale entry was written to DB and shown in the Skills page
after switching tabs. Fixed with four-layer defence:

1. scan_skill_catalog FS→DB sync: skip skills with name "unnamed"
2. scan_skill_catalog DB→FS reverse sync: actively delete "unnamed" DB entries
3. list_skills: filter out "unnamed" entries before returning to frontend
4. lib.rs startup sync: skip "unnamed" and clean up any pre-existing ones

Also bump version to 0.5.2 and update README changelog.
